@model DataAccessLayer.Models.RoleViewModel

@{
    ViewData["Title"] = "Role";
}

<hr />
<div class="row">
    <div class="col-md-4">
        @if (ViewBag.Message != null)
        {
            <div class="p-1 mb-2 bg-success text-white">@ViewBag.Message</div>
        }
        @if (ViewBag.error != null)
        {
            <div class="p-1 mb-2 bg-danger text-white">@ViewBag.error</div>
        }
        <form asp-action="CreateRole">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
             @Html.Partial("_CreateEditRole")
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-warning mt-3">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}